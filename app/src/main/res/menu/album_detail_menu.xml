<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- orderInCategory 优先级越大，会被挤压到overflow上显示 -->
    <item
        android:id="@+id/action_favor_item"
        android:icon="@mipmap/favorite_selected"
        android:orderInCategory="10"
        android:title="@string/favor_selected"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/action_unfavor_item"
        android:icon="@mipmap/favorite_normal"
        android:orderInCategory="10"
        android:title="@string/favor_norma"
        app:showAsAction="always"
        />
</menu>